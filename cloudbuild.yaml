steps:
  # Build the container image
  - name: 'gcr.io/cloud-builders/docker'
    args:
      - 'build'
      - '-t'
      - 'europe-west1-docker.pkg.dev/$PROJECT_ID/aparte/admin-dashboard:$SHORT_SHA'
      - '-t'
      - 'europe-west1-docker.pkg.dev/$PROJECT_ID/aparte/admin-dashboard:latest'
      - '--build-arg'
      - 'NEXT_PUBLIC_NODE_ENV=${_NEXT_PUBLIC_NODE_ENV}'
      - '--build-arg'
      - 'NEXT_PUBLIC_BASE_API_URL=${_NEXT_PUBLIC_BASE_API_URL}'
      - '--build-arg'
      - 'NEXT_PUBLIC_GA4_MEASUREMENT_ID=${_NEXT_PUBLIC_GA4_MEASUREMENT_ID}'
      - '--build-arg'
      - 'NEXT_PUBLIC_CLARITY_PROJECT_ID=${_NEXT_PUBLIC_CLARITY_PROJECT_ID}'
      - '.'

  # Push the container image to Artifact Registry
  - name: 'gcr.io/cloud-builders/docker'
    args:
      - 'push'
      - '--all-tags'
      - 'europe-west1-docker.pkg.dev/$PROJECT_ID/aparte/admin-dashboard'

  # Deploy container image to Cloud Run
  - name: 'gcr.io/google.com/cloudsdktool/cloud-sdk'
    entrypoint: gcloud
    args:
      - 'run'
      - 'deploy'
      - '${_SERVICE_NAME}'
      - '--image'
      - 'europe-west1-docker.pkg.dev/$PROJECT_ID/aparte/admin-dashboard:$SHORT_SHA'
      - '--region'
      - 'europe-west1'
      - '--platform'
      - 'managed'
      - '--allow-unauthenticated'
      
      # Resource allocation
      - '--memory'
      - '512Mi'
      - '--cpu'
      - '1'
      - '--timeout'
      - '300'
      
      # Environment variables
      - '--update-env-vars'
      - 'NEXT_PUBLIC_NODE_ENV=${_NEXT_PUBLIC_NODE_ENV},NEXT_PUBLIC_BASE_API_URL=${_NEXT_PUBLIC_BASE_API_URL}'

images:
  - 'europe-west1-docker.pkg.dev/$PROJECT_ID/aparte/admin-dashboard'

substitutions:
  _SERVICE_NAME: 'admin-dashboard'
  _NEXT_PUBLIC_NODE_ENV: 'production'
  _NEXT_PUBLIC_BASE_API_URL: 'https://api.aparteng.com/api/v1'
  # Analytics ids — set per-environment in the Cloud Build trigger's
  # substitutions. Blank on staging: src/lib/analytics.ts additionally requires
  # NEXT_PUBLIC_NODE_ENV=production, so a leaked id there would still load
  # nothing, but leaving them blank keeps that a belt AND braces.
  _NEXT_PUBLIC_GA4_MEASUREMENT_ID: ''
  _NEXT_PUBLIC_CLARITY_PROJECT_ID: ''

options:
  logging: CLOUD_LOGGING_ONLY
  defaultLogsBucketBehavior: REGIONAL_USER_OWNED_BUCKET

timeout: 1200s
